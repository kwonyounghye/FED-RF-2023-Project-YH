import { menu } from "../data/menu";
import { MenuList } from "./MenuList";

export function MenuBox(props) {
    // props.cat - 카테고리명 -> 데이터 선택 객체 속성명

    // 선택 데이터 : 카테고리에 해당하는 데이터를 가져옴!
    const selData = $('.menu img');
    // 메뉴박스 보이기 함수
    const showBox = () => {
        console.log("이미지: ");
    }
}